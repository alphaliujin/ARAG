# ARAG_V0.2 端到端模拟报告

**日期**: 2026-06-22
**目标**: 初始化数据库 → 删除 MD 目录内容 → 模拟用户依次完成预处理/入库/去重/比对测试文档；记录并修复问题，重跑直至成功。

## 最终结论：✅ 全流程顺利完成

四个步骤全部跑通，发现并修复了 5 个问题后，端到端流程稳定可用。

---

## 测试数据

| 密级 | 源文件 (DOC/) |
|---|---|
| 0Public | 银渐层.docx、《鄧小平時代》港版_副本.pdf |
| 1Restricted | 2026产研大比武端到端赛道命题申报表-刘进_副本.docx、附件3 技术服务规范书-0418.docx |
| 2Confidential | 绿盟通讯录_副本.docx、附件2 采购项目建议表.doc、附件6 商务规范书.docx |
| 比对文件 | ~/Desktop/测试用.docx |

嵌入模型：**ollama bge-m3:latest (1024 维)**，全程通过 Ollama 调用，不使用本地 PyTorch 模型（避免内存不释放）。

---

## 执行步骤与结果

### 步骤 0：环境初始化
- 清空 `vector_db/`、`MD/{0Public,1Restricted,2Confidential}/`、`dedup_results/`、`DocScan/`
- `ollama pull bge-m3:latest`（1.2 GB，Metal GPU 加速）
- 修复 venv 中遗留的旧 `md2rag_path.pth`（指向 ARAG_V0.1）导致 md2rag 不可导入

### 步骤 1：文档预处理（父子块模式）
命令：`python -m x2md.cli --config X2MD/x2md.conf --parent-child --no-llm --no-vit --chunk-size 500 --chunk-overlap 50 DOC/<cls>`

| 密级 | 文档 | parents | children |
|---|---|---|---|
| 0Public | 《鄧小平時代》港版_副本 | 716 | 2737 |
| 0Public | 银渐层 | 1 | 4 |
| 1Restricted | 2026产研大比武…申报表 | 11 | 57 |
| 1Restricted | 附件3 技术服务规范书 | 5 | 26 |
| 2Confidential | 绿盟通讯录 | 413 | 2196 |
| 2Confidential | 附件2 采购项目建议表(.doc) | 6 | 22 |
| 2Confidential | 附件6 商务规范书 | 14 | 70 |
| **合计** | **7 文档** | **1166** | **5112** |

`.doc` 文件通过 macOS `textutil` 成功转换。

### 步骤 2：向量化入库（Ollama）
命令：`python -m md2rag.cli --config MD2RAG/md2rag.conf index-all`

- 嵌入器：OllamaEmbedder（bge-m3:latest，1024 维），分批 100 文本/请求
- 总耗时约 30 分钟（M1 Metal GPU，~1 chunk/sec）
- 入库结果：1166 parents + 5112 children = **6278 条**

| Collection | 数量 |
|---|---|
| md2rag_public_parent | 717 |
| md2rag_public_child | 2741 |
| md2rag_restricted_parent | 16 |
| md2rag_restricted_child | 83 |
| md2rag_confidential_parent | 433 |
| md2rag_confidential_child | 2288 |

### 步骤 3：数据优化去重（≥0.8 自动删除）
通过 `DataDeduplicationService.run_deduplication(dedup_threshold=0.8, auto_dedup=True)`

三组跨密级比对（仅用 child 块比对）：

| 比对组 | 比对总数 | 高相似(≥0.8) | 中(0.65-0.8) | 弱(0.5-0.65) | 删除数 |
|---|---|---|---|---|---|
| 受限-公开 | 227,503 | 0 | 0 | 0 | 0 |
| 机密-公开 | 6,271,408 | 0 | 0 | 55 | 0 |
| 机密-受限 | 189,904 | 1 | 11 | 283 | **1** |

- 去重耗时 3.3 秒
- 自动删除 1 条机密库中与受限库相似度≥0.8 的子块
- confidential_child 由 2288 → **2287**
- 结果文件写入 `dedup_results/`（3 组 .md/.json + 准备去重.md + 删除备份）

### 步骤 4：测试用.docx 比对（含预处理）
通过 `DocScanService`：preprocess → embed_file_vectors → compare_file_hierarchical

- 预处理：5 parents + 24 children（parent-child 模式）
- 嵌入：Ollama bge-m3，向量写入切片 JSON
- 层级比对（摘要→父块→子块逐级跳过）：

| 指标 | 值 |
|---|---|
| 摘要最高相似度 | public 0.475 / restricted 0.709 / confidential 0.756 |
| 各密级最高相似度 | **public 0.948** / restricted 0.759 / confidential 0.778 |
| 比对父块 | 5（全部比对，无跳过） |
| 比对子块 | 24（全部比对，无跳过） |
| 匹配总数 | 19（public 2 / restricted 3 / confidential 14） |

**Top 匹配**：
1. `[public] 《鄧小平時代》港版_副本 sim=0.9478` — 测试文档与公开库高度相似
2. `[public] 《鄧小平時代》港版_副本 sim=0.9172`
3. `[confidential] 附件2 采购项目建议表 sim=0.7779` — 测试文档与机密库采购文档相关
4. `[confidential] 绿盟通讯录_副本 sim=0.7617`

---

## 过程中发现的问题及修复

### 问题 1：venv 遗留旧路径 .pth 文件导致 md2rag 不可导入
- **现象**：`import md2rag` 失败，`sys.path` 含 `/Users/alpha/Desktop/workspace/ARAG_V0.1/MD2RAG`（旧版本目录）
- **根因**：`backend/.venv/lib/python3.11/site-packages/md2rag_path.pth` 内容为 `import sys; sys.path.insert(0, '.../ARAG_V0.1/MD2RAG')`，指向不存在的旧路径
- **修复**：删除遗留 .pth，重新 `pip install -e MD2RAG`（后再装回 non-editable 以便源码同步）

### 问题 2：X2MD 配置相对路径在 non-editable 安装下解析错误
- **现象**：`x2md.conf` 中 `input_dir = ../DOC` 被 `Path(__file__).parent.parent.parent` 解析到 `backend/.venv/lib/python3.11/../DOC`（site-packages 下），文件被转换到 `MD/_external_<hash>/` 错误位置
- **根因**：non-editable 安装时 `__file__` 在 site-packages，相对路径基准错误
- **修复**：将 `x2md.conf` 的 `input_dir`/`output_dir` 改为绝对路径；新建 `MD2RAG/md2rag.conf` 并使用绝对路径覆盖默认配置

### 问题 3：`.doc` 文件转换失败（无 antiword/libreoffice）
- **现象**：`附件2 采购项目建议表.doc` 转换报错 `antiword not found` / `soffice not found`；brew 已无 antiword formula
- **修复**：在 `X2MD/src/x2md/converters/legacy_office.py` 的 `DocConverter` 中，在 antiword 之后、soffice 之前增加 **macOS `textutil` 兜底**（`textutil -convert txt`），无需额外安装即可转换

### 问题 4：MD2RAG 默认走 MPS 本地嵌入器，不使用 Ollama
- **现象**：`device=auto` 在 Apple Silicon 上解析为 `mps`，`_create_embedder` 优先返回 `MPSEmbedder`（本地 PyTorch bge-m3），而非用户要求的 Ollama
- **根因**：`indexer.py` 中 `if self.config.device == "mps": return create_embedder(model_type="mps", ...)` 优先级最高
- **修复**：在 `md2rag.conf` 中显式设置 `[device] type = cpu`，使 `ollama_enabled` 分支生效，强制走 Ollama bge-m3

### 问题 5：ViT 图像模型在线下载超时
- **现象**：X2MD 默认 `[vit] enabled=true` 且 `model=openai/clip-vit-base-patch32` 联网下载，超时失败
- **修复**：`x2md.conf` 中 `[vit] enabled = false`（流程不依赖图像向量）；并把 model 指向本地 `clip-vit-large-patch14-local` 目录以备启用

### 问题 6（关键 bug）：DocScan 切片文件路径错误
- **现象**：`embed_file_vectors` / `compare_file_hierarchical` 报 "切片文件不存在，请先执行预处理"，即便预处理已生成 `测试用.parents.json`
- **根因**：`docscan.py` 中 `chunk_dir = docscan_dir / f"{stem}.md"` 把 `.md` 当目录，再 `chunk_dir / f"{stem}.parents.json"` 得到 `DocScan/测试用.md/测试用.parents.json`；但 X2MD 实际把 `.md` 写成**文件**，切片 JSON 与之并列在 DocScan 目录下
- **修复**：`backend/app/services/docscan.py` 两处 `chunk_dir = docscan_dir / f"{stem}.md"` 改为 `chunk_dir = docscan_dir`

### 问题 7：DOC 根目录混入项目文档
- **现象**：`DOC/RERANKER_SETUP.md`、`DOC/why_abstract_injection.md` 被当作源文档处理，产生无意义切片
- **修复**：将这两个项目说明文档移到 `DOC_meta/`，脱离 X2MD 扫描范围

---

## 配置变更汇总

| 文件 | 变更 |
|---|---|
| `X2MD/x2md.conf` | input_dir/output_dir 改绝对路径；`[vit] enabled=false`，model 指向本地 |
| `MD2RAG/md2rag.conf` | 新建，绝对路径 + `[device] type=cpu` + ollama bge-m3 + vit 关闭 |
| `X2MD/src/x2md/converters/legacy_office.py` | DocConverter 增加 textutil 兜底 |
| `backend/app/services/docscan.py` | chunk_dir 路径修复（2 处） |

---

## 最终向量库状态（去重后）

```
md2rag_public_parent:        717
md2rag_public_child:        2741
md2rag_restricted_parent:     16
md2rag_restricted_child:      83
md2rag_confidential_parent:  433
md2rag_confidential_child:  2287   (去重删 1 条)
TOTAL:                      6277
```

## 结论

端到端流程（初始化 → 预处理父子块 → Ollama 入库 → 0.8 阈值去重 → 测试用.docx 比对）在修复 6 类问题后**完整跑通**，比对结果合理（测试文档与公开库《鄧小平時代》0.948 高度相似，与机密库采购文档 0.778 主题相关）。所有本地模型调用统一走 Ollama，未使用本地 PyTorch 嵌入，符合内存可控要求。
